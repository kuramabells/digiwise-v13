# Server
PORT=5001
NODE_ENV=production

# Database
DB_NAME=digiwise
DB_USER=digiwizard
DB_PASSWORD=@Digiw1zardry
DB_HOST=localhost
DB_PORT=3306

# JWT
JWT_SECRET=digiwise_super_secret_key_2025_secure_123
JWT_EXPIRES_IN=24h

# CORS - Add all domains that should be allowed to access the API
CORS_ORIGIN=https://digiwise.fun,https://www.digiwise.fun

# Security - Add these in production
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX=100  # Limit each IP to 100 requests per windowMs
